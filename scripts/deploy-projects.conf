# name,supabase-project-ref — order is load-bearing: Tineka always attempts
# before Opeke, per Rule 3 (test before touching production).
#
# Sandbox (kifqftelvliywqkizsho) intentionally removed 2026-07-26 — it was a
# temporary workaround for Supabase's old free-tier 2-project limit and is no
# longer needed on the current paid plan. Only 2 real projects exist now.
tineka,zfefukxaliuximizjkwa
opeke,cbeenkpjpnhmtqtnjiyd
